import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // =========================================================
  // BACKGROUND
  // =========================================================

  static const Color background = Color(0xFF1A1630);
  static const Color headerBackground = Color(0xFF53467F);
  static const Color moon = Color(0xFFFDF3D0);

  // =========================================================
  // TEXT
  // =========================================================

  static const Color heading = Color(0xFFF7F0D8);

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Color(0xFFD0C9DC);

  // =========================================================
  // BUTTON GRADIENT (FROM FIGMA)
  // =========================================================

  static const Color buttonGradientStart = Color(0xFFBA68C8);
  static const Color buttonGradientEnd = Color(0xFF5B3362);

  // =========================================================
  // GLASS UI
  // =========================================================

  // Base white glass color
  static const Color glassWhite = Colors.white;
  static const Color cardBackground = Color(0x1F2F2B45);

  // Purple outline used across the app
  static const Color purpleBorder = Color(0xFFA78BFA);

  // =========================================================
  // EXTRA COLORS (Future Screens)
  // =========================================================

  static const Color success = Color(0xFF6EDC8C);

  static const Color warning = Color(0xFFFFC857);

  static const Color danger = Color(0xFFFF5E78);

  static const Color shadow = Colors.black;
}